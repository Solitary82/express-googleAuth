nav
  div.brandContainer
    a(href="/")
      i.fas.fa-adjust
      | main-auth!
  div.signInContainer
    if (logged)
      p.signed
        | Signed as
        br
        span #{email}
      a(href='/logout')#logout Logout
    else
      a(href='../auth/google')#signIn Sign in
